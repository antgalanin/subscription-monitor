# Скопировать в .env рядом с docker-compose.prod.yml и заполнить.
# Файл .env на сервере в git не попадает.
DOMAIN=example.com
POSTGRES_DB=subscription_monitor
POSTGRES_USER=subscription_app
POSTGRES_PASSWORD=
APP_INIT_ADMIN_PASSWORD=
APP_TAG=v1.0.0
